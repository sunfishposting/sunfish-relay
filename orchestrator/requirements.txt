# Sunfish Relay Orchestrator Dependencies

# Config parsing
pyyaml>=6.0

# OBS WebSocket (for OBS monitor)
websocket-client>=1.6.0

# HTTP requests (for future REST-based monitors)
requests>=2.31.0

# QR code generation (for signal-cli linking)
qrcode>=7.4
